Move Harvest timesheets to Toggl
=============

Convert Harvest CSV files to the format, that Toggl can import.

Example:

    $ python 2toggl.py < sample/harvest_exported.csv > sample/toggl_import.csv
